{
  "name": "minimal-shadow-cljs-release",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "shadow-cljs": "^0.16.1"
  },
  "scripts": {
    "del": "rm -rf dist/*",
    "compile": "yarn del && shadow-cljs compile app",
    "build": "yarn del && shadow-cljs release app"
  }
}
