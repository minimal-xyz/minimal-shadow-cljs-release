{
  "name": "minimal-shadow-cljs-release",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.11.1",
    "shadow-cljs": "^2.8.39"
  },
  "scripts": {
    "del": "rm -rf dist/*",
    "compile": "shadow-cljs compile app",
    "release": "shadow-cljs release app",
    "build": "yarn del && yarn release"
  }
}
